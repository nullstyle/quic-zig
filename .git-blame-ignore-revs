# Revisions listed here are mechanical restyle commits (de-indents,
# rename-only moves). git blame skips them when configured:
#   git config blame.ignoreRevsFile .git-blame-ignore-revs
# GitHub's blame UI honors this file automatically.
